

# Update Required Field Green to #10D806

Single-line change in two files — update the `rgba` color from `(17, 236, 6)` to `(16, 216, 6)`:

1. **`src/components/kpi/KpiInputColumn.tsx` line 56** — change to `rgba(16, 216, 6, 0.15)`
2. **`src/components/kpi/DataGrid.tsx` line 64** — change to `rgba(16, 216, 6, 0.15)`

